# PROGRESS.md - invest-dash 진행상황

> 새 세션 시작 시 날짜 입력 + CLAUDE.md + 이 파일 전달

---

## 현재 상태

**단계: 전체 파이썬 엔진 통합 완료, 3탭 대시보드 운영 중 (포트폴리오 탭 관리자 전용) + PWA(B&K) + 이메일 발송 추가 + 연금 시세 위젯 + 포트폴리오 GS 연동 재구축**

접속 URL: https://dalbonz.github.io/invest-dash
마지막 업데이트: 2026-06-22

**앱 이름: B&K** (PWA 홈화면 설치 시 표시되는 이름)

---

## 완료

- GitHub Actions 자동화, cron 시각을 정각/30분에서 오프셋 (아래 스케줄 참고)
- market.py: us10y(^TNX), us30y(^TYX), candles 포함 / pykrx 투자자별 순매수(KRX 로그인 필요, 미해결)
- ai_summary.py: Sonnet 4.6, **볼드** 형식, **섹션당 4~5개 항목으로 분량 확대**
  - **내 보유종목(holdings) 섹션 추가**: 포트폴리오 종목명이 market.py 시세 키와 매칭되면 자동으로 액션아이템 생성 (NAME_TO_KEY 매핑, 매칭 없으면 빈 문자열로 안전하게 스킵 — 할루시네이션 방지)
- news.py: 6개 RSS 소스 (속보/분석/국내/국제) + AI 요약(Haiku)
  - HEADERS를 풀브라우저 UA로 교체 (매일경제 Cloudflare 403 차단 해결)
  - 파이낸셜뉴스 RSS 경로 변경 대응 (`/rss/r20/fn_realnews_*.xml`)
  - 국제뉴스 소스 추가 (fn_realnews_international.xml) → UI 필터 '미국'→'국제'로 변경
- youtube.py: RSS로 영상탐색 + Supadata 자막 + AI 요약 (미디어채널 3개 + 개인채널 3개)
  - 개인채널은 오늘 영상만 사용 (당일 KST 기준 필터)
  - 자막 있으면 [섹션명]+불릿 구조 요약(투자/free 포맷), 자막 없으면 제목 기반 요약
  - 중복 호출 방지: data.json의 기존 videoId와 동일하면 자막 재호출 없이 기존 요약 재사용
  - "12시에 만나요" 채널 titleKeyword 필터링 (같은 채널의 다른 시간대 프로그램과 구분)
  - temperature=0 고정
- notify.py: 텔레그램(아침브리핑+신규영상) + **이메일 발송 추가** (Gmail SMTP, HTML 다이제스트)
  - **무음시간 가드** `in_quiet_hours()`: KST 22:00~07:00에는 텔레그램 발송 보류 (data.json은 갱신)
- run.py: 모드 분기 (yt/morning/email/full) — **모든 모드에서 신규영상 체크+알림 통일 적용** (기존엔 yt모드만 알림)
- index.html 3탭 구조 (포트폴리오 관리자 전용)
  - 탭1 투자브리핑: AI 섹션 들여쓰기 개선, holdings 섹션 표시 추가
  - 탭2 증시: 기준선 항상 표시, 한국/미국 버튼 타이틀 인라인, K/A 뱃지
  - 탭2 증시: 상승/하락분야 → 테마탭 (반도체/AI/소부장/로봇/조선/금융/바이오)
  - 탭3 미디어: 뉴스 accordion, **유튜브 요약 가독성 개선** (섹션별 색상: 매수=초록/매도주의=빨강/오늘액션=보라, 좌측 컬러바)
  - 관리자 버튼 업데이트 시간 왼쪽 배치
- **PWA 추가**: manifest.json + service-worker.js + 아이콘(B&K, icons/) → "홈 화면에 추가" 가능
- requirements.txt: pykrx 추가
- 이메일 실사용 검증 완료 (2026-06-18): GMAIL_USER/PASSWORD/EMAIL_RECIPIENTS 등록 완료, 발송 확인됨
  - 제목/본문 헤더를 "Invest Dash 브리핑" → "{날짜} 투자브리핑"으로 변경
  - 가독성 개선: **볼드**→`<strong>`, +/-수치 자동 색상, 항목별 줄바꿈 (`_fmt_ai_section`/`_fmt_yt_section`)
  - AI시황 무음실패 수정: max_tokens 4500→7000, timeout 60→75, 실패시 로그 출력 추가
- 수동 워크플로 실행시 신규영상 알림 중복발송 방지 (workflow_dispatch면 텔레그램 보류)
- data.json push 충돌 재시도 로직 추가 (pull --rebase 최대5회)
- 수동 force_email 입력으로 이메일 즉시 테스트 가능 (workflow_dispatch inputs)

---

## 현재 index.html 탭 구조

| 탭 | 제목 | 내용 |
|---|---|---|
| 1 | 투자 브리핑 | AI시황(7섹션,들여쓰기) + 주요시장8개(WTI포함) |
| 2 | 증시 | 4카드(기준선sparkline) + 주식시세(테마탭,K/A뱃지) + ETF |
| 3 | 미디어 분석 | 뉴스(accordion+AI요약) + 유튜브(기본닫힘+펼침) |
| - | 포트폴리오 | 관리자→포트폴리오 보기로만 진입 (비번 1111) |

---

## GitHub Actions 실행 스케줄 (2026-06-18 변경)

cron을 정각/30분에서 오프셋 — GitHub가 공식 권장하는 방식. 정각/30분에 전세계 cron이 몰려서
**2.5~5시간 지연**되는 게 실제 로그로 확인된 원인이었음 (이전엔 08:30 브리핑이 11~13시에 도착).
추가로 investment-news-bot의 워크플로가 정확히 같은 시각(`30 23 * * *`)에 같은 계정에서 돌고 있어
지연을 가중시키고 있었음 → 비활성화 권장 (본인이 직접: Actions 탭 → Disable workflow).

| UTC | KST | 모드 | 내용 |
|---|---|---|---|
| 22:05 | 07:05 | yt | 개인채널 유튜브 체크 (무음시간 종료 직후, 밤사이 영상 첫 알림) |
| 23:08 | 08:08 | yt | 개인채널 유튜브 체크 |
| 23:22 | 08:22 | morning | 전체 실행 + 아침 브리핑 텔레그램 발송 (장 시작 09:00 전) |
| 01:12 | 10:12 | yt | 개인채널 유튜브 체크 |
| 06:07 | 15:07 | email | 전체 실행 + 이메일 발송 (12시에 만나요 처리 후) |
| 09:14 | 18:14 | full | 전체 실행 (텔레그램/이메일 없음, 신규영상은 알림) |

무음시간(KST 22:00~07:00)에는 어느 모드든 신규영상 텔레그램 알림이 보류됨 (data.json은 갱신).

---

## 2026-06-19 작업 내용

- **외부 cron 연동(cron-job.org) 추가**: GitHub Actions 네이티브 `schedule:`가 정각/30분 몰림으로 지연되는 문제를 외부 cron 서비스로 우회
  - `update.yml`에 `workflow_dispatch.inputs.slot` 추가 (yt1/yt2/yt3/morning/email/full)
  - `run.py`에 `SLOT_TO_MODE` 매핑 + `get_mode()`가 SLOT 우선 사용, `is_test_run()`으로 순수 수동클릭만 알림 보류
  - cron-job.org에 6개 잡 등록 완료 (invest-morning/yt1/yt2/yt3/email/full), 전체 테스트 204 확인
- **버그 발견 및 수정: 중복 발송 + 유튜브 요약 누락**
  - 원인: cron-job.org 추가 후에도 `update.yml`의 구버전 `schedule:` 크론 6개가 같이 남아있어서, 같은 시간대에 워크플로가 2번(네이티브 스케줄 + 외부 dispatch) 실행됨
  - 증상: 텔레그램/이메일 중복발송, Supadata 자막 중복호출로 한쪽 실행은 자막을 못 받아 제목만으로 부실 요약
  - 조치: `update.yml`의 `on.schedule` 블록 전체 제거 → 이제 cron-job.org가 스케줄 단일 소스 (커밋 `2acc5ad`)
  - **확인 필요**: 내일 아침부터 중복 없이 1번만 오는지 실제 발송 결과로 검증
- YouTube RSS 간헐적 차단(404/파싱오류) 대응: `_fetch_rss`에 재시도 로직 추가 (channel_id 경로는 최대 3회, user= 경로는 1회)
- 텔레그램 브랜딩 통일: 아침 브리핑 제목 "Invest Dash 아침 브리핑" → "{날짜} 투자브리핑 (B&K)" (이메일 제목은 어제 이미 "{날짜} 투자브리핑"으로 변경됨, 단 이메일엔 "(B&K)" 미반영 — 통일 필요시 추가 작업)
- **중복발송 수정 검증**: cron-job.org로 `slot=full` 1회 수동 트리거 → GitHub Actions 실행도 정확히 1개만 생성됨 확인 (커밋 `2acc5ad` 정상 작동, `gh run list`로 확인)
- **로고 텍스트 변경 완료**: "Invest Dash" → "B&K Investment" (index.html 헤더, 커밋 `ec337a0`)
- **사이트 ↔ 텔레그램 데이터 동기화 이슈 발견 및 수정**: 원인은 `loadData()`가 페이지 로드시 1회만 호출되고 자동 갱신이 없어서, 텔레그램으로 새 알림이 와도 이미 열어둔 탭은 갱신 전 상태로 남아있던 것 (서버/배포 문제 아님, GitHub Pages는 정상적으로 매번 최신 반영됨)
  - 조치: KST 08:00~22:00 사이에 30분마다 `data.json` 자동 재조회 + 미디어탭이 열려있으면 뉴스/유튜브 재렌더링 (커밋 `2ca6056`)

---

## 2026-06-21 작업 내용

- **텔레그램 브리핑 제목 변경**: "{날짜} 투자브리핑 (B&K)" → "{날짜} B&K 투자리포트" (`engines/notify.py`)
- **모바일 레이아웃 버그 수정**
  - 증시탭 카드 헤더(한국/미국, 실시간/5일/1달/1년 버튼)가 좁은 화면에서 글자가 두 줄로 깨지던 문제 → `flex-wrap`+`white-space:nowrap`으로 수정
  - 투자브리핑/미디어분석/헤더 탭의 가로폭이 서로 달라서 화면 전환시 들쑥날쑥하던 문제 → 증시 탭 기준(`max-width:1400px`)으로 전부 통일
- **가독성 개선**: 투자브리핑(AI 시황 각 섹션 제목/본문) 및 미디어분석(뉴스/유튜브 제목) 폰트 크기·줄간격 확대
- **연금 시세 위젯 신규 추가** (증시 탭, 주요 ETF 하단)
  - 개인연금/퇴직연금 2개 타일, 각각 "내 개인"/"회사 지원" 토글로 4개 리스트 관리
  - 야후파이낸스 티커로 실시간 조회(주요 ETF와 동일 방식), 관리자 메뉴(증시 설정)에서 종목 추가·삭제
  - 티커 없는 MMF/현금성 자산은 금액을 직접 입력해 고정값으로 표시 가능
  - 종목 추가 시 기존 등록 종목/ETF 자동완성으로 티커 자동 입력
- **포트폴리오 구글시트 연동 구조적 문제 발견 → 아키텍처 재정립** (가장 큰 작업)
  - 발견 1: `buildOwnerData`가 항상 브라우저 localStorage를 구글시트보다 우선해서 보여주던 버그 (1차 수정)
  - 발견 2(더 큰 문제): 포트폴리오 탭이 쓰던 Google Apps Script가 `주식현황상세`/`예적금내역`/`입출금통장`(달봉즈님이 실제로 관리하는 시트)을 전혀 안 읽고, `Stocks`/`Cash`/`Savings`라는 별도의 빈 자동생성 탭만 읽고 쓰고 있었음 — 두 시스템이 완전히 분리되어 있던 것
  - 발견 3: Apps Script를 재작성해 실제 시트 3개를 직접 읽도록 고친 뒤 테스트하다가, **구글시트 워크북 전체가 셀 개수 한도(1,000만개)를 초과**해서 쓰기(저장)가 항상 실패하고 있던 것을 발견 (`_RAW` 트렌드 추적 탭들이 누적되며 한도에 다다른 것으로 추정 — 시트 자체 정리 필요, 코드로는 해결 불가)
  - **최종 아키텍처 결정**: 구글시트 자동 동기화를 포기하고, **웹(localStorage)을 포트폴리오의 단일 편집 창구**로 전환
    - 포트폴리오 탭의 모든 표시/계산은 이제 localStorage 기준 (구글시트 연동 여부와 무관하게 항상 동일하게 동작)
    - "저장" 시 구글시트로 자동 전송하던 로직 제거
    - **"시트에서 가져오기"**: 처음 한 번만 시트의 기존 데이터(주식 16종목 등)를 웹으로 채워주는 1회용 버튼
    - **"구글시트에 백업"**: 원할 때 수동으로 현재 웹 데이터를 시트(Stocks/Cash/Savings 탭)로 내보내는 버튼 (백업 목적, 실패해도 웹 데이터엔 영향 없음)
    - 시트(`ref`) 값과 웹 값이 다르면 참고용으로 빨간 점(●) 표시는 유지 (자동 덮어쓰기 없음)
  - **확인 필요**: 구글시트 자체의 셀 한도 문제 정리(`_RAW` 탭 정리), "시트에서 가져오기"/"구글시트에 백업" 버튼 실사용 테스트, 예적금내역 탭 데이터 유무 확인

---

## 2026-06-22 작업 내용

- **"코드가 저장 안 된 것 같다" 점검**: PWA + 포트폴리오(Apps Script 연동) 작업이 안 보인다는 보고로 git 전체(dangling commit/stash/원격) 점검
  - 결론: 2026-06-21 작업(`84ab739`, `5909d5f` 커밋)은 **전부 정상 저장되어 있었음** — localStorage 단일편집 구조, "시트에서 가져오기"/"구글시트에 백업" 버튼, 보기화면의 빨간 점(●) 불일치 표시 모두 코드에 존재 확인
  - 종목이 안 보였던 진짜 원인: localStorage는 브라우저/기기별로 분리 저장되는 설계(어제 의도된 구조) → PC 브라우저에 입력한 데이터는 다른 기기/브라우저에는 원래 안 보임. 매번 타이핑하는 대신 **"시트에서 가져오기"** 버튼 사용 권장
  - **실제로 빠져있던 것 1개**: 보기화면엔 시트 불일치 빨간 점(●)이 있었지만 **관리자 편집(edit) 화면**에는 없었음 → 추가함 (qty/avg_price/amount/rate_pct 불일치 시 해당 입력칸 빨간색 + hover 툴팁로 참고시트 값 표시, 커밋 `d6d425d`)
- **교훈**: 코드스페이스 작업 후 반드시 커밋해야 하며, 닫히면 워킹트리 변경분이 통째로 사라짐 — 단 이번 건은 실제로는 다 커밋되어 있었고 확인 과정에서 새로 찾은 사용자 가이드(아래)가 진짜 원인이었음
- **구글시트 백업 실패 원인 진단**: "구글시트에 백업" 버튼이 계속 실패 → Apps Script `doGet`에 `?diag=1`/`?trim=1` 진단 기능을 임시로 추가해 원인 추적
  - 원인: 워크북 전체 셀 1,000만 개 한도 중 `주가트렌드_RAW`(28,155행×300열, 실제 데이터는 3,959행×9열)와 `환율트렌드_RAW`(50,500행×26열, 실제 데이터는 5행×3열)가 빈 칸만으로 97.7% 차지
  - 조치: 두 탭의 빈 행/열만 정리(`trimRawSheets`, 데이터 손실 없음) → 약 970만 셀 확보 확인
  - **백업은 트림 이후에도 여전히 실패** — 원인이 셀 한도가 아닐 가능성 있음, 추가 조사 필요 (다음 할일 참고)
  - Apps Script 전체 코드는 `apps_script_full.txt`로 레포에 보관 (다시 잃어버리지 않도록)
- **포트폴리오 코드 구조화 — `portfolio.js` 분리**: index.html에 흩어져 있던 포트폴리오(주식/현금/예적금) 관련 함수 전체를 별도 파일로 통합 (커밋 `57ee851`)
  - localStorage가 항상 단일 소스이고, 구글시트 연동은 파일 안에서 "=== Google Sheets 연동 시작/끝 ===" 구역으로 명확히 분리
  - 향후 구글시트 연동을 완전히 포기하고 싶으면 그 구역만 삭제하면 독립 동작 가능하도록 설계 (달봉즈님이 반복되는 시트 오류에 지쳐서 요청한 대안)
  - index.html은 `<script src="portfolio.js">`로 연결, 기존 동작 동일 (함수 누락 없음 확인)

---

## 2026-06-22 (오후) 작업 내용 — 최우선 긴급 수정 5건

달봉즈님이 "프로젝트 근간을 부정당할 수 있는 수준"이라며 5개 항목을 최우선 지시. 전부 조사 완료, 핵심 2건 즉시 수정.

1. **데이터 정확성 (가장 심각, 수정 완료)**: AI는 할루시네이션한 게 아니었음 — 받은 숫자를 그대로 인용. 진짜 원인은 `market.py`가 쓰는 **야후파이낸스의 `chartPreviousClose`(전일종가) 메타필드가 부정확**했던 것.
   - 검증: 달봉즈님이 제시한 실측값(네이버금융) — 코스피 9114.55, SK하이닉스 2,922,200원(+5.72%) — 와 비교
   - 수정 전: 코스피 9090.83, SK하이닉스 2,895,000원(**+26.53%**, 완전히 틀린 등락률)
   - 원인: 같은 API 응답의 캔들(차트) 데이터엔 정확한 전일종가(2,764,000원)가 있는데, 메타필드만 틀렸음
   - 조치: `fetch_price()`가 메타필드 대신 **캔들의 전일 종가**를 우선 사용하도록 변경 (전 종목/지수 공통 적용)
   - 수정 후 재검증: 코스피 9114.55(네이버와 완전 일치), SK하이닉스 +5.61%(네이버 5.72%와 거의 일치) — 커밋 `ca719d3`
   - **이 버그는 전 종목/지수에 동일하게 영향(S&P500/나스닥 등락률도 같이 보정됨)** — 달봉즈님이 "다른것도 다 마찬가지"라고 확인해주심
2. **미디어탭이 비어있던 문제 (수정 완료)**: data.json의 `youtube` 배열이 0개였음. 원인은 `run.py`의 `run_full()`이 유튜브 RSS가 **전체 채널 차단(404/500)** 시 빈 배열로 기존 데이터를 그냥 덮어쓰던 버그. `run_yt_only()`와 동일하게 기존 데이터와 병합하도록 수정 — 커밋 `ca719d3`
3. **텔레그램 불규칙 발송**: 미디어채널(TV) 신규영상도 텔레그램 발송하는 게 의도된 동작(달봉즈님 확인) — AI요약 포함해서 보내는 것까지 기존 코드가 이미 하고 있음, 별도 수정 불필요. (단, 위 2번 버그로 인해 일부 영상이 누락됐던 것이 원인 중 하나였을 수 있음 — 지켜봐야 함)
4. **아침 브리핑 시간 08:22 → 08:30 변경**: 코드가 아니라 cron-job.org 외부설정이라 달봉즈님이 직접 변경 필요 — console.cron-job.org/jobs → invest-morning → EDIT → 08:30으로 수정 (안내만 드림, 아직 본인 액션 대기)
5. **유료 서비스 점검**: 코드 내 모든 API 키는 실제 사용 중(불필요한 키 없음). Anthropic(필수, 사용량과금) / Supadata(선택, 없어도 제목기반 요약 자동대체) / cron-job.org($17/월, 코드 밖 외부서비스라 본인이 직접 대시보드에서 무료플랜 충분한지 확인 필요 — 잡 6개면 보통 무료범위일 가능성 높음)
6. **추가 지시 — 야후를 왜 쓰는지 + 그래프 기준선 위치 문제** (1번을 더 근본적으로 해결)
   - **국내 지수/종목 데이터 소스를 야후 → 네이버금융으로 전환** (커밋 `e74f38c`): 야후는 한국 종목 데이터 품질이 떨어짐이 이미 확인된 상태라, 로그인 없이 한국거래소 데이터를 그대로 미러링하는 네이버금융 실시간 API로 1차소스 교체. 코스피/코스닥/국내종목 8개 전부 적용, 실패시에만 야후 폴백. 미국/환율/원자재는 야후 그대로 유지(검증 문제없었음)
   - **증시탭 차트 기준선 위치 수정** (커밋 `cc24fcd` → `3ce84a2`에서 보정): 처음엔 기준선을 강제로 정중앙에 고정했다가, 달봉즈님이 네이버 차트 캡처로 "기준선 아래 빈 공간 인위적으로 만들지 말라"고 정정해주심 → 실제 데이터 없는 쪽을 인위적으로 부풀리지 않고, 가장자리에 완전히 붙어 안 보이지 않을 정도의 최소 여백(15%)만 확보하도록 재수정 (네이버 스타일과 동일)
   - **알아둘 점(후속 과제)**: 브라우저에서 직접 야후를 호출하는 부분(실시간 카드 기간전환, ETF, 종목 상세차트의 `yfetch`)은 아직 야후 그대로임 — CORS 때문에 브라우저에서 네이버를 직접 호출하기 어려워 서버(market.py)만 우선 전환함. 이 부분도 같은 chartPreviousClose 버그에 노출될 수 있어 다음 우선순위로 점검 필요
7. **투자브리핑에 날짜 명시**: "AI 시황" 카드 제목 옆에 "(2026년 6월 22일 기준)" 형태로 직접 표시 (기존엔 페이지 맨 아래 작은 글씨로만 있어서 눈에 안 띄었음)
8. **미디어탭 재설계 — "오늘(00시~23:59 KST) 전체 영상" 기준** (커밋 `3ce84a2`): 기존엔 채널당 최신 영상 1개만 유지(덮어쓰기)하던 구조라 그날 올라온 영상들이 계속 사라졌음
   - RSS의 모든 entry 중 오늘(KST) 발행분을 전부 수집, 기존 요약은 재사용(`isNew:false`)하고 신규만 새로 요약(`isNew:true`)
   - data.json의 기존 오늘자 영상과 병합 → 자정 지나면 어제 영상 자동 제외(리셋), 일부 채널 RSS 차단시에도 그날 이미 수집된 영상은 유지
   - 웹에서 최신순 정렬 + 신규영상 빨간 New 뱃지
   - **⚠️ 비용 영향 확인 필요**: 미디어채널(TV) 3곳이 하루 10~15개씩 영상을 올림 — 기존엔 채널당 1개/일만 AI요약했는데 이제 전부 요약하게 되어 Anthropic 호출이 수십 배 늘어남. 실사용 며칠 지켜보고 비용 부담되면 미디어채널만 제목기반 요약으로 낮추는 등 조정 필요

---

## 다음 작업 (미완료, 우선순위 순)

0. **08:30 cron-job.org 설정 변경** (달봉즈님 액션 필요, 위 참고)
0-2. **cron-job.org 무료/유료 플랜 확인** (달봉즈님 액션 필요, 위 참고)
0-3. **브라우저 직접호출 yfetch(ETF/상세차트/카드 기간전환)도 야후 chartPreviousClose 버그 노출 가능성 점검** (위 6번 후속과제 참고)
1. **구글시트 백업 실패 원인 추가 조사** — 셀 한도(`_RAW` 탭) 정리했는데도 "백업 실패" 계속됨. 한도 문제가 아니라면 doPost 자체 오류(권한/시트보호/다른 한도 등) 의심, `apps_script_full.txt` 기반으로 추가 디버깅 필요
   - 참고: 달봉즈님이 구글시트 연동 자체를 포기하는 옵션도 고려 중 → `portfolio.js`를 그 가능성 고려해서 구조화해둠 (위 작업내용 참고)
2. **포트폴리오 실제 동작 검증** (portfolio.js 분리 후) — 보기/편집/저장/시트가져오기/시트백업이 기존과 동일하게 동작하는지 브라우저에서 직접 확인
3. **안드로이드 실기기에서 2026-06-21 변경사항 전체 검증** — 아직 미완료
   - 모바일 카드 헤더 버튼 줄바꿈 수정 확인 (PWA 캐시 때문에 안 보이면 앱 완전 종료 후 재실행 또는 재설치)
   - 탭 간 가로폭 통일 확인
   - 연금 시세 위젯: 종목 추가 → 실시간 가격 정상 조회되는지
4. **미디어 탭 채널 편집 기능** (관리자 메뉴에서 유튜브/뉴스 채널 추가·삭제·수정)
5. **PWA 사용법 확인**: 와이프 폰 등 다른 기기에서도 동일하게 설치되는지 추가 확인 (본인 폰은 설치 성공 확인됨)
6. **실사용 확인**: cron-job.org 스케줄대로 텔레그램/이메일이 중복 없이 1번만 오는지 며칠 더 지켜보기

### 그 외 미완료 (우선순위 낮음)
- 기관/외국인/개인 pykrx 실패 원인 파악 (data.json에 investors 없음, KRX_ID/PW 필요한 것으로 보임)
- investment-news-bot 워크플로 비활성화 (본인 액션 필요, 위 스케줄 참고 — 아직 안 하셨으면 확인)
- holdings 액션아이템: 현재 NAME_TO_KEY에 등록된 8개 종목만 매칭됨 → 포트폴리오 실제 종목과 비교해 매핑 확장 필요
- **Supadata 플랜 결정 대기** (아래 참고)
- 예적금내역 시트 실제 데이터 확인 (현재 빈 상태로 조회됨)
- Kyoung 시트도 동일한 구조(주식현황상세 등)인지 확인 후 Kyoung용 Apps Script도 같은 방식으로 수정 필요

---

## 시스템 완성도 평가 (2026-06-18 기준, 자체 진단)

| 영역 | 완성도 | 비고 |
|---|---|---|
| 시장데이터 | 90% | investor 데이터(pykrx)만 미작동 |
| 뉴스 | 90% | 소스 6개 복구 완료 |
| AI 시황 | 88% | 분량 확대 + holdings 스캐폴딩 완료 |
| 유튜브 | 88% | 자막+필터+dedup 완료 |
| 텔레그램 | 85% | cron 지연 원인 파악, 오프셋 적용 |
| 이메일 | 70% | 코드 완료, secrets 등록 대기 |
| 관리자 패널 | 90% | |
| PWA | 80% | manifest+SW+아이콘 완료, 실기기 설치 테스트 필요 |
| 포트폴리오 GS연동 | 40% | 백엔드만 있고 프론트 미검증 (별도 작업 예정) |

investment-news-bot/YT_sumbot 대비 시장·뉴스·유튜브는 기능 우위 확인, 이메일은 새로 구현,
포트폴리오만 기존 대비 검증 부족 상태.

---

## Supadata 자막 API — 크레딧 이슈 (2026-06-18)

- 기존 Google Apps Script(YT_sumbot)가 매시간(24회/일) × 6채널로 Supadata를 호출해 Free플랜(월100크레딧) 소진
- 호출 테스트 결과 `429 limit-exceeded`, **리셋일: 2026-07-13**
- Apps Script 트리거 비활성화 권장 (크레딧 회복은 안 되지만, 리셋 후 invest-dash와 중복 소모 방지)
  - 비활성화: 스크립트 에디터 → 시계 아이콘(트리거) → `runScheduled` 옆 ⋮ → 삭제
  - **복구 방법**: 스크립트 에디터에서 함수 선택창에 `setupTrigger` 선택 → 실행(▶) → 매시간 트리거 자동 재생성됨 (코드에 이미 있는 함수, 별도 기억 불필요)
- youtube.py에 중복호출 방지 추가됨(위 참고) → 실제 소모량은 "워크플로 실행횟수"가 아니라 "신규 영상 개수"에 비례
- 플랜 선택 보류 중 (Basic $/월 300크레딧 vs Pro 3000크레딧) — 중복방지 적용 후 실사용량 보고 결정 예정

---

## 텔레그램 발송 설정

Secrets 설정 완료 (2026-06-15):
- `TELEGRAM_BOT_TOKEN`: YT_sum_bot 토큰 설정됨
- `TELEGRAM_CHAT_IDS`: `8649321702,8799181333` (달봉즈, 경)

모드별 동작:
- **yt/morning/email/full 전체**: 새 영상 감지 시 텔레그램 발송 (무음시간 22~07시는 보류)
- **morning**: 아침 브리핑 (AI요약 + 주요지수 + 뉴스헤드라인 + 링크)
- **email**: AI시황 전체 + 시장표 + 유튜브요약 + 뉴스 HTML 다이제스트 (Gmail SMTP)

주의: 봇 토큰이 채팅에 노출됨 → BotFather에서 `/token` 재발급 권장

이메일 설정 (Secrets 등록 필요):
- `GMAIL_USER`: 발신 Gmail 주소
- `GMAIL_PASSWORD`: Gmail 앱 비밀번호 (일반 로그인 비번 아님, Google계정→보안→앱비밀번호에서 발급)
- `EMAIL_RECIPIENTS`: 쉼표구분 수신자 목록

---

## 주요 설정값

| 항목 | 값 |
|---|---|
| 포트폴리오 비번 | 1111 |
| SHA-256 해시 | 0ffe1abd1a08215353c233d6e009613e95eec4253832a761af28ff37ac5a150c |
| 관리자 비번 | 1111 (동일) |
| 관리자 진입 | 로고 5번 탭 |
| Bong GS URL | https://script.google.com/macros/s/AKfycbw1W851igwvCuIzoghZyKfgkpqsgc2oPxNHrHsJhDLWhQbKHvlxQGTqMzLuImqnImHv/exec |
| AI 모델 (유튜브/뉴스요약) | claude-haiku-4-5-20251001 |
| AI 모델 (시황) | claude-sonnet-4-6 |

---

## 파일 구조

```
invest-dash/
├── engines/
│   ├── market.py      완료 (us10y/us30y/candles 포함)
│   ├── news.py        완료 (category + AI 요약)
│   ├── portfolio.py   완료 (GS 연동)
│   ├── ai_summary.py  완료 (7섹션 프롬프트)
│   ├── youtube.py     완료 (RSS+AI, 개인채널 오늘필터)
│   ├── notify.py      완료 (텔레그램 발송)
│   └── __init__.py
├── .github/workflows/
│   └── update.yml     완료 (KST 6/8/8:30/10/15/18시)
├── index.html         완료 (3탭 + 포트폴리오 + PWA 등록)
├── run.py             완료 (yt/morning/email/full 모드)
├── manifest.json      완료 (PWA, 앱이름 B&K)
├── service-worker.js  완료 (네트워크우선 캐싱)
├── icons/             완료 (icon-192.png, icon-512.png)
├── requirements.txt   완료
├── data.json          자동생성 (keys: updated/market/news/portfolio/ai/youtube)
├── CLAUDE.md          완료
└── PROGRESS.md        이 파일
```

---

## data.json 스키마 (확정)

```json
{
  "updated": "ISO8601 UTC",
  "market":  { "kospi": {...}, "sp500": {...}, ... },
  "news":    [ { "title", "link", "source", "category", "summary" }, ... ],
  "portfolio": { "bong": {...}, "kyoung": {...}, "total": {...} },
  "ai":      { "summary", "kr", "us", "sectors", "holdings", "picks", "do", "dont" },
  "youtube": [ { "name", "type", "videoId", "title", "updated", "summary" }, ... ]
}
```

---

## 포트폴리오 Google Sheets 연동 설정 방법

현재 `portfolio.py`는 GS에서 데이터를 읽지만, UI의 포트폴리오 탭은 GS에서 직접 읽지 않고 Apps Script URL을 통해 읽음.

**A안 (권장): data.json 경유 방식**
- portfolio.py에서 GS 데이터를 읽어 data.json의 `portfolio.bong`, `portfolio.kyoung`에 저장
- UI에서 `jsonData.portfolio`를 직접 사용하도록 renderPortfolio 수정
- Apps Script 불필요

**현재 상태:**
- GOOGLE_SHEETS_ID: 설정됨
- GOOGLE_SERVICE_ACCOUNT_JSON: 설정 여부 확인 필요

---

## 에러 이력 (반복 방지)

| 에러 | 원인 | 해결 |
|---|---|---|
| api_status_404 | 모델명 오류 | claude-haiku-4-5-20251001 사용 |
| WorksheetNotFound | 탭 이름 불일치 | 주식현황상세 (정확히) |
| Actions push 403 | workflow permissions | Read and write 설정 |
| UTC 이중변환 | toKST 헬퍼 내부에서 +9h 중복 | ISO8601 그대로 JS Date에 넘기도록 수정 |
| 유튜브 할루시네이션 | 설명 부족 시 AI가 내용 생성 | 설명 30자 미만이면 제목만 반환 |
| handle 미해결 | YouTube @handle → channel_id 실패 | 다중 URL/패턴 시도 로직 강화 |
| rainbow -74% 데이터 오류 | 277810.KS(KOSPI)로 잘못 설정 | 277810.KQ(KOSDAQ)으로 수정 |
| Pages 공백 | index.html 미커밋으로 구 버전 서비스 | 전체 변경사항 수동 커밋 필요 |
| loadETF JS 문법 오류 | `}}}}catch` 여분 중괄호 | `}}}catch`로 수정 |
| 매일경제 RSS "not well-formed" | Cloudflare가 기본 UA 차단(403, 챌린지 HTML 반환) | 풀브라우저 User-Agent로 교체 |
| 파이낸셜뉴스 RSS 404 | RSS 경로가 `/rss/r20/fn_realnews_*.xml`로 변경됨 | URL 갱신 |
| 12시에만나요 영상 오매칭 | RSS 최신 1건만 사용 → 다른 시간대 프로그램 섞임 | RSS 전체 entry 순회 + titleKeyword 필터 |
| Supadata 429 limit-exceeded | 기존 Apps Script가 매시간 호출해 Free플랜(월100) 소진 | 유료 플랜 업그레이드 + 중복호출 방지 추가 |
| 08:30 브리핑 지연(2.5~5시간) | cron이 정각/30분에 몰려 GitHub 큐 혼잡 + investment-news-bot이 동일 시각 cron 운영 | 모든 cron을 비정각으로 오프셋, investment-news-bot 워크플로 비활성화 |
| 수동실행시 신규영상 텔레그램 중복발송 | "모든 모드 알림 통일" 적용 후 workflow_dispatch도 알림 대상 포함됨 | GITHUB_EVENT_NAME=workflow_dispatch면 신규영상 알림만 보류 |
| data.json push 충돌(non-fast-forward) | 여러 실행이 거의 동시에 끝나 git push 경쟁 | pull --rebase 후 재시도 루프(최대5회) 추가 |
| 이메일 "AI 시황 데이터 없음" (조용한 실패) | 섹션 길이 확대 후 응답이 max_tokens(4500)에서 잘려 JSON 파싱 실패, 콘솔에 에러도 안 찍힘 | max_tokens 7000으로 확대 + 실패 분기에 print 로그 추가 |
| 이메일에 **볼드** 마크다운 그대로 노출 | text.replace('\n','<br>')만 적용, 볼드/수치색상 처리 없음 | _fmt_ai_section/_fmt_yt_section 추가 (대시보드 fmtAI와 동일 로직) |

---

## 참고 레포

- https://github.com/Dalbonz/investment-news-bot
- https://github.com/Dalbonz/4-my-money
